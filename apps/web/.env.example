# Copy to .env.local and adjust for your environment.
NEXT_PUBLIC_API_BASE_URL=https://mednexa-api-dev.salmoncoast-083126b7.centralus.azurecontainerapps.io

# Optional: UUID used only when GET /facilities returns no rows (dashboard census hint).
# NEXT_PUBLIC_DEMO_FACILITY_ID=
